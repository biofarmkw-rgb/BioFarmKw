{{ block.settings.custom_liquid }}
{% schema %}
{
  "name": "t:blocks._custom_liquid.name",
  "class": "custom-liquid-block",
  "settings": [
    {
      "type": "liquid",
      "id": "custom_liquid",
      "label": "t:sections.all.custom_liquid.label",
      "info": "t:sections.all.custom_liquid.info"
    }
  ],
  "presets": [
    {
      "name": "t:blocks._custom_liquid.presets.name"
    }
  ]
}
{% endschema %}
